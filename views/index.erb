<h3><%= session[:remaining_guesses] %> guesses left</h3>
<h2>Make a guess:</h2>
<% unless session[:guessed_letters].empty? %>
  <p>
    Your guesses:
    <br> 
    <%= session[:guessed_letters] %>
  </p>
<% else %>
  <br/><br/>
<% end %>
<p>
  <form action='/try' method='post'>
    <input type='text' name='guess' title='Enter a letter.' autofocus required />
    <input type='submit' value='Go' />
  </form>
</p>